# Frame sequences — asset contract

This directory is **owned by Script 3**. Script 1 (the build) consumes whatever
is here through the manifest in `lib/sequences.ts`. Widening `widths` when new
renditions land must require no change anywhere else.

Everything currently on disk is a **§9 placeholder**, generated by
`scripts/gen-placeholder-frames.py`. It is gitignored — the generator is
tracked, its output is not. Regenerate with:

```bash
python3 scripts/gen-placeholder-frames.py
```

---

## File layout

```
public/seq/<id>/<width>/frame_0001.webp    1-based, zero-padded to 4
public/seq/<id>/poster.webp                the representative still
```

`<id>` is one of `hero`, `case-01`, `case-02`, `case-03`.

---

## Required renditions

| id | aspect | frames | widths needed |
|---|---|---|---|
| `hero` | 16:9 | 180 | **375, 768, 1440** |
| `case-01` | 16:10 | 60 | **375, 960** |
| `case-02` | 16:10 | 60 | **375, 960** |
| `case-03` | 16:10 | 60 | **375, 960** |

Only the largest of each currently exists. A 375px phone therefore downloads
**1.34 MB** of 1440-wide hero frames, which bears directly on §10's throttled
mobile item. `pickWidth()` already selects the narrowest rendition that covers
the element — it just needs the files.

Add narrower widths to the `widths` array in `lib/sequences.ts` as they land.
Ascending order.

---

## The safe area — read this before shooting or rendering

Each sequence declares a `safeArea` in the manifest: the fraction of the frame
that **must stay visually quiet** because DOM copy sits over it.

| id | safe area | what is on top of it |
|---|---|---|
| `hero` | x 0 → 0.60, y 0.22 → 0.96 | The headline, bottom-left, at `--text-mega` |
| `case-*` | full width, y 0.62 → 1.0 | Case metadata on mobile, where it stacks |

**Quiet means:**

- No hard edges, no lettering, no high-frequency detail
- Luminance low enough that bone `#EDEAE3` clears **4.5:1** over it
- A dark graded field, soft gradient, or out-of-focus material is right
- A bright subject, a horizon line, or type is not

**Why this exists.** The placeholder frames collided with the headline —
`HERO 001 / 180` printed straight through the words "that feel" at every
breakpoint, in the normal render, the reduced-motion render *and* the
JS-disabled render.

**The vignette does not save you.** It is
`radial-gradient(circle at 50% 45%, transparent 30%, var(--color-void) 92%)`,
which darkens the *corners* and leaves the frame centre at full strength. The
headline crosses that transparent centre band at every breakpoint. It is a mood
device, not a legibility device.

The last row of the viewport belongs to the fixed timecode status bar. It is
opaque, so nothing needs to be reserved in the frame for it — but do not put
anything you care about in the bottom `--timecode-lane` (2.5rem), because it
will simply be covered.

---

## Encoding

- **WebP.** No `<video>`, no HLS, no GIF, no Lottie — spec C1 and C2.
- Budget the whole sequence, not the single frame. The hero is 180 frames; at
  1440 the placeholder set is already 1.34 MB.
- Frames stream in order, so favour consistent per-frame size over a small
  average with spikes.
- The poster is what renders under `prefers-reduced-motion`, in `<noscript>`,
  and before the sequence loads. It must stand alone as a still and satisfy the
  same safe area.

---

## Definition of done for Script 3

- [ ] All four sequences present at every width in the table above
- [ ] A poster per sequence
- [ ] Bone text verified ≥ 4.5:1 over the safe area **on the busiest frame**,
      not just frame 1
- [ ] `widths` updated in `lib/sequences.ts`, ascending
- [ ] Re-run the §10 throttled-mobile check afterwards — this is the one
      acceptance item real frames can regress
