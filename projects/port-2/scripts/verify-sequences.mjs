#!/usr/bin/env node
// Placeholder — a later agent replaces this with the real frame-sequence
// verifier (parity with port-1's scripts/verify-frames.py, adapted for this
// project's manifest format).
console.log("verify-sequences: not yet implemented");
process.exit(0);
